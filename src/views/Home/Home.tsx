import styles from './Home.module.css';

const Home = () => {
	return (
		<div className={styles.home}>
			Use the search field at the top to search for your desired show.
		</div>
	);
};

export default Home;
